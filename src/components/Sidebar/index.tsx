
import styles from './style.module.scss';

const Sidebar = () => {
    return (
        <div className={styles.container}>
            <div>Sidebar</div>
        </div>
    );
};

export default Sidebar;
